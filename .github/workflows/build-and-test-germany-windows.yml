name: Build and test - Germany (Windows)

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: windows-latest
    
    env:
      DOTNET_NOLOGO: true
      DOTNET_CLI_TELEMETRY_OPTOUT: true
      
    defaults:
      run:
        working-directory: src/Germany/Mews.Fiscalizations.Germany.Tests
        
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.x

    - name: Clean
      run: dotnet clean ./Mews.Fiscalizations.Germany.Tests.csproj --configuration Release && dotnet nuget locals all --clear
        
    - name: Install dependencies
      run: dotnet restore
    
    - name: Build
      run: dotnet build --configuration Release --no-restore
    
    - name: Test
      env:
        german_client_Id: ${{secrets.german_client_Id}}
        german_tss_id: ${{secrets.german_tss_id}}
        german_api_key: ${{secrets.german_api_key}}
        german_api_secret: ${{secrets.german_api_secret}}
        german_admin_pin: ${{secrets.german_admin_pin}}
        german_admin_puk: ${{secrets.german_admin_puk}}
      run: dotnet test --no-restore --verbosity normal
